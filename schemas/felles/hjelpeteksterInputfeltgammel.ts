import { DokumentNavn, Steg } from '../typer';
import localeBlockForStegDokument from './localeBlockForStegDokument';

const hjelpeteksterInputfeltgammel = [
  localeBlockForStegDokument(Steg.FELLES, DokumentNavn.TEKSTER_FOR_DATO),
];

export default hjelpeteksterInputfeltgammel;
