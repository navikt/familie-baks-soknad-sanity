import { DokumentNavn, Steg } from '../typer';
import localeStringForStegDokument from './localeStringForStegDokument';

const feiltekster = [localeStringForStegDokument(Steg.FELLES, DokumentNavn.FEILTEKSTER)];

export default feiltekster;
